import { StyleSheet } from "react-native";

import { Colors } from "~/theme/colors";

export default StyleSheet.create({
    label: {
        color: Colors.grey,
    },
    infoBlock: {
        marginBottom: 4,
        flexDirection: 'row'
    },
})